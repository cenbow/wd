package com.okwei.bean.enums;

public enum PriceVisibleTypeEnum {
	   /**
	    * 认证员 （保证金）
	    */
		ProxyPrice(1),
		/**
		 * 认证点
		 */
		FloorPrice(2);
		
	    private final int step; 
	    private PriceVisibleTypeEnum(int step) { 

	        this.step = step; 
	   }
	   @Override
	   public String toString()
	   {
	   	return String.valueOf(this.step);         	
	   }
}
